; Increase upload limits for menu QR (PDF + restaurant image + product images).
; Used by PHP when running as CGI/FPM (e.g. many shared hosts).
upload_max_filesize = 64M
post_max_size = 68M

; Security hardening
expose_php = Off
display_errors = Off
log_errors = On
